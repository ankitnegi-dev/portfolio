import { NextResponse } from "next/server";

// Placeholder real implementation calls the FastAPI RAG service in Phase 5.
export async function POST() {
  return NextResponse.json({
    reply: "Assistant not wired up yet coming in Phase 5.",
  });
}
